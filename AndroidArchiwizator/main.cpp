#include "mainwindow.h"
#include <QApplication>
#include "server.h"
int main(int argc, char *argv[])
{
    // reb
    QApplication a(argc, argv);
    MainWindow w;
    w.show();
    return a.exec();
    // finalna
    // anka
}
